# ==========================================
# SIEPA Backend - Environment Variables
# ==========================================
# Copy this file to .env and fill in the values
# Never commit .env to version control

# ==========================================
# REQUIRED - Application
# ==========================================
NODE_ENV=production

# Server port (Render sets this automatically)
PORT=5000

# JWT secret for authentication tokens
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_change_me

# CORS allowed origins (comma-separated)
# Production: https://siepa.vercel.app
# Development: http://localhost:5173,http://localhost:3000
CORS_ORIGIN=https://siepa.vercel.app

# ==========================================
# REQUIRED - Database (Neon PostgreSQL)
# ==========================================
# Get this from Neon dashboard: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/siepa?sslmode=require

# ==========================================
# AI / OCR Services
# ==========================================

# Replicate API token (DeepSeek-VL vision model)
# Get from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_your_replicate_token_here

# DeepSeek AI API key (for pedagogical explanations)
# Get from: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-your_deepseek_key_here

# Replicate model (optional, defaults to deepseek-ai/deepseek-vl-7b-base)
# REPLICATE_DEEPSEEK_VL_MODEL=deepseek-ai/deepseek-vl-7b-base

# OCR microservice URL (only needed if running separate OCR service)
# OCR_SERVICE_URL=http://localhost:8001

# ==========================================
# Optional
# ==========================================

# MongoDB connection (legacy - only if using Mongoose alongside Prisma)
# MONGO_URI=mongodb://localhost:27017/siepa
